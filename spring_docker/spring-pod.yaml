apiVersion : v1
kind : Pod
metadata :
  name : spring-pod-1
  labels :
    app : spring-app
spec :
  containers :
    - name : spring-container
      image : spring-server
      ports :
        - containerPort : 8081
      imagePullPolicy : IfNotPresent


---

apiVersion : v1
kind : Pod
metadata :
  name : spring-pod-2
  labels :
    app : spring-app
spec :
  containers :
    - name : spring-container
      image : spring-server
      ports :
        - containerPort : 8081
      imagePullPolicy : IfNotPresent

---
apiVersion : v1
kind : Pod
metadata :
  name : spring-pod-3
  labels :
    app : spring-app
spec :
  containers :
    - name : spring-container
      image : spring-server
      ports :
        - containerPort : 8081
      imagePullPolicy : IfNotPresent